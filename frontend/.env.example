# Optional. Leave unset in dev to use Vite proxy: same-origin /api → http://127.0.0.1:8000
# If you call the backend directly (no proxy), include the /api prefix:
# VITE_API_URL=http://127.0.0.1:8000/api
